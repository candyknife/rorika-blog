---
title: "深度学习服务器环境配置指南 2：训练环境"
date: 2023-01-07
updated: 2023-06-10
categories: [技术]
tags: [PyTorch, 服务器]
---

## 一、Miniconda 安装

1. 下载安装包
   [Miniconda](https://docs.conda.io/en/latest/miniconda.html)

2. 验证哈希值

   ```bash
   sha256sum filename
   ```

3. 安装

   ```bash
   bash Miniconda3-latest-Linux-x86_64.sh
   ```

4. 重启终端并测试

   ```bash
   conda list
   ```

5. 更新

   ```bash
   conda update conda
   ```

## 二、项目环境配置

根据需要为项目创建对应环境。

```bash
conda create -n project_name
```

激活项目环境。

```bash
conda activate project_name
```

## 三、Pytorch 安装

查看当前 Nvidia 驱动版本。

```bash
nvidia-smi
```

查找对应的 cuda 版本：[CUDA documentation](https://docs.nvidia.com/cuda/cuda-toolkit-release-notes/index.html#title-resolved-issues)。
根据 cuda 版本执行对应的安装指令：[PyTorch](https://pytorch.org/get-started/locally/)。

```bash
conda install pytorch torchvision torchaudio pytorch-cuda=11.7 -c pytorch -c nvidia
```

## 四、Jupyter Notebook 安装

安装：

```bash
conda install notebook
```

生成配置文件并修改：

```bash
jupyter notebook --generate-config
vi ~/.jupyter/jupyter_notebook_config.py
```

单独打开一个终端窗口用以生成密钥：

```bash
python
```

```python
from notebook.auth import passwd
passwd()

Enter password:
Verify password:
'argon2:$argon2id$...'
```

在 Jupyter Notebook 的配置文件中改动以下内容：

```python
c.NotebookApp.allow_remote_access = True
c.NotebookApp.open_browser = False
c.NotebookApp.notebook_dir = './jupyter'
c.NotebookApp.password = u'argon2:$argon2id$...'
c.NotebookApp.password_required = True
c.NotebookApp.ip = '*'
c.NotebookApp.port = 7777
```

> 更新于 2023-06-10
